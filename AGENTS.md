# 仓库开发规范

适用于整个仓库；子目录 `AGENTS.md` 可补充或覆盖对应子树规则。

## 优先级与工作范围

Primus FHE 是持续开发中的 Rust workspace，暂不承诺稳定 API。依次优先：

1. 数学正确性以及表示、布局不变量。
2. 热路径无额外分配、少分支，并利于编译器优化。
3. API 简单，明确所有权、布局和临时工作区契约。
4. 实现可读、可维护，关键控制流和数值不变量能局部理解。
5. 测试、示例、基准和文档少而聚焦。

- 开始前检查 `git status` 和相关暂存、未暂存差异，保留用户修改与暂存状态。
- 只做最小且完整的修改。允许实质改善项目的破坏性 API 变更，但须同步 workspace 调用方、测试、示例、基准和文档，不增加无实际需要的兼容层。
- “规划、分析、审查”本身不授权修改源码；遵循会话中已有的实现授权。删除本次变更产生的废弃代码和导入，无关问题只报告。
- Workspace 成员见根 `Cargo.toml` 的 `crates/*` 和 `xtask`；跨层修改从底层向调用方推进。`primus_data`、`primus_gcd` 是基础 crate。
- `temp/` 中的 OpenFHE、SEAL 等仅供参考，不作为 Primus 源码修改。`.cargo/config.toml` 已设置 `target-cpu=native`，命令和基准注释中不重复设置。

## API、抽象与文档

- 优先复用现有类型、普通函数、固有构造器、泛型/trait 和切片 API。新抽象须集中真实不变量、消除已确认重复、防止实际误用、隔离本质不同的表示，或有可测量收益；参数重复或存在分支本身不足以证明需要新类型。不为假想调用方预留接口。
- NTT、Fourier、CRT/DCRT 等表示可独立；不以复杂公共 trait 隐藏关键数值差异。宏仅在稳定重复、生成不变量或必要编译期工作确有收益时保留，权衡展开、诊断和维护成本。
- 名称表达数学角色和表示，同一角色跨 crate 使用一致术语。同一函数族保持词干及 `try_`、`lazy_`、`*_assign`、`*_to`、`*_slice` 的语义一致；契约不同不强行同名。
- 参数名表达 `input`、`lhs`、`rhs`、`addend`、`acc`、`output`、`scratch`、`context`、`modulus` 等角色；同一 API 层和函数族保持顺序一致，不强求跨层单一顺序。
- 构造器、访问器和纯计算在丢弃结果通常是错误时使用 `#[must_use]`；原地操作不添加。
- 公开 API 和非平凡内部函数按需说明职责、输入假设、不变量、表示转换和算法原因。签名不能表达的范围、归一化状态、输出位置、累加器清零责任和工作区要求进入文档；不为机械转发或显然的一行 helper 复述代码。
- `# Panics` 写实际检查或转换导致的失败；`# Correctness` 写未检查的数学、表示和范围前提，不暗示违反前提必然 panic。说明可能的部分写入；继承底层超出 trait 保证的前提时引用原契约。
- 同目录的 `README.md` 与 `README.zh_CN.md` 同步维护，语言入口、章节结构和链接对应。

## 数值正确性与检查边界

- 修改算术前确认输入域、输出范围、表示、布局、归一化状态及精确缓冲区长度；检查溢出、窄化、移位、wrapping 和惰性约简前提。
- 在拥有契约的最高层公开、批量或构造边界集中验证调用方数据，向私有 kernel 传递已建立的不变量。不要逐层重复验证长度、模数、basis 或 table，也不在逐系数内层循环添加重复检查或调度分支。
- 数值内核通常仅以少量边界 `debug_assert!` 检查内部不变量；重要且由安全公开 API 独立承担的检查可保留在 release 边界。内存安全条件必须在 release 成立，不能依赖 `debug_assert!` 保护 unsafe 访问。
- 主动 panic 提供明确消息，避免调用方可触发的 `unreachable!`。对实现相同数学操作的 scalar、SIMD、NTT、Fourier、CRT/DCRT、RNS 路径核对语义并做聚焦差分检查，不强行统一表示。

## 性能与基准

- 先选清晰且利于优化的实现，再测量热路径；不假定 iterator、索引或 unsafe 必然更快。复用表示、预计算和 scratch/context，避免在线重复分配、`Vec`、`clone`、`collect`、变换及虚假的零长度缓冲区。
- 已知可整除时优先 `chunks_exact` / `chunks_exact_mut`；在内层循环外选择 scalar/SIMD 或特化 kernel。公开 wrapper 负责检查与调度，私有 kernel 专注算术并记录前提。
- 使用 Criterion。每次迭代测量一个清楚定义的工作负载，不为增加样本手动重复；setup、分配若非测量对象应移出计时 closure。
- 比较等价工作，固定参数、CPU、工具链和 feature；group/case 名称、必要的 throughput 与 `black_box` 应反映真实工作。声称热路径改善前必须测量。
- 保留能回答稳定性能问题的基准，删除本轮调查结束后无长期价值的实验。常用命令可放对应 benchmark 注释；nightly SIMD 命令只放 SIMD benchmark 中。

## 测试与示例

- 只增加保护独立契约、回归或持久诊断价值的最少测试；优先固定 seed、确定性输入、简单独立 oracle、表驱动或差分覆盖，避免概率性断言和不稳定阈值。
- 公开行为放在 `tests/`；私有 kernel 测试仅在提供独立诊断价值时与实现同处。普通测试不放 benchmark、调试输出或大型统计实验。
- 仅在本次目标范围内整理已有测试/基准，区分真正重复与独立边界覆盖。实现任务可删除机械转发、标准库行为和无长期价值的重复测试；纯审查只提出建议。完整 crate 整理才系统评估其全部资产。
- 删除本轮仅用于调查或证明当前实现的临时测试。示例展示推荐的公开工作流，不为覆盖率增加重复案例。

## 审查与状态文档

- 用户要求 Rust 代码审查、复审或基于源码的重构分析时，使用仓库 `primus-review` skill。由 skill 按改动审查、定向分析、完整复审确定覆盖深度和委派方式；常规实现验证或仅依据已有报告编写计划不自动升级为完整复审。
- 审查完成依赖请求范围内的源码、契约、调用方及验证资产覆盖，不能由命令通过代替。小任务用简短覆盖说明，完整复审使用覆盖账本。
- 恢复工作时先读 `HANDOFF.md` 当前状态，再按目标读取相关决定。HANDOFF 只保留当前范围、有效边界、未决项和下一步；更新时替换过时内容，不追加日志，不复述本规范。
- 公开契约进入 rustdoc/README，局部算法理由进入源码/benchmark 注释；无法就地表达的跨层决定按需链接参考。实现与提交历史由 Git 记录，历史验证需按当前任务重跑，既有性能选择可在前提改变后复测。

## 验证与交付

实现任务先做窄范围验证，再按风险扩大：

```text
cargo fmt --all
cargo check -p <crate> --all-targets
cargo test -p <crate>
cargo clippy -p <crate> --all-targets -- -D warnings
```

- 纯审查不格式化写入；需要格式检查时用 `cargo fmt --all -- --check`。纯文档任务检查内容和链接，按需构建文档，不机械运行全量数值测试。
- Workspace 验证使用 `just fmt-check`、`just check`、`just lint`、`just test`、`just simd`；SIMD 需要 nightly。先确认 recipe/feature 覆盖实际目标，缺失时补显式包命令。无关失败如实报告，不削弱检查或顺手修改。
- 交付前检查最终 diff，清理本轮临时实验；说明行为/API 变化、实际验证及有意未验证路径。
- 未经明确要求，不 stage、unstage、commit、amend 或 push。Commit 使用简洁 Conventional Commit，不添加协作者元数据；无关修改拆分提交。
